//1. Оператор И(&&) Конъюнкция
//2. Оператор ИЛИ(||) дизъюнкция
//3. Оператор НЕ(!) инверсия

int main(){
    bool a = true;
    bool b = false;
    bool aorb = a || b;
    bool nota = !a;
    bool aandb = a && b;
}

